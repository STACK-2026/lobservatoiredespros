---
import BaseLayout from "../layouts/BaseLayout.astro";
import Seal from "../components/Seal.astro";
import CounterLive from "../components/CounterLive.astro";
import EditorialHeader from "../components/EditorialHeader.astro";
import Medaille from "../components/Medaille.astro";
import ProCard from "../components/ProCard.astro";
import PullQuote from "../components/PullQuote.astro";
import TelescopeWatermark from "../components/TelescopeWatermark.astro";
import InkBleedDivider from "../components/InkBleedDivider.astro";
import InfoPill from "../components/InfoPill.astro";
import ScoreCircle from "../components/ScoreCircle.astro";
import GlossaireTerm from "../components/GlossaireTerm.astro";
import { siteConfig } from "../utils/config";

// Classements populaires , 8 combinaisons métier × dept
const classementsPopulaires = [
  { metier: "Plombier", slug: "plombier", dept: "Paris", deptSlug: "paris-75", count: 142 },
  { metier: "Électricien", slug: "electricien", dept: "Paris", deptSlug: "paris-75", count: 198 },
  { metier: "Couvreur", slug: "couvreur", dept: "Yonne", deptSlug: "yonne-89", count: 38 },
  { metier: "Menuisier", slug: "menuisier", dept: "Côte-d'Or", deptSlug: "cote-dor-21", count: 54 },
  { metier: "Isolation", slug: "isolation", dept: "Yonne", deptSlug: "yonne-89", count: 29 },
  { metier: "Plombier", slug: "plombier", dept: "Côte-d'Or", deptSlug: "cote-dor-21", count: 67 },
  { metier: "Électricien", slug: "electricien", dept: "Yonne", deptSlug: "yonne-89", count: 41 },
  { metier: "Couvreur", slug: "couvreur", dept: "Côte-d'Or", deptSlug: "cote-dor-21", count: 33 },
];

// "À la Une" , 3 observations vedettes
const alaUne = [
  {
    category: "ENQUETE",
    title: "Dans l'Yonne, la fin programmee des fausses mentions RGE",
    excerpt: "Six entreprises referencees ont perdu leur label en six mois. Notre redaction a verifie les certifications affichees sur 412 sites web d'artisans.",
    author: "Camille Fabre",
    date: "19 avril 2026",
    readingTime: 12,
    slug: "enquete-rge-yonne-412-sites",
  },
  {
    category: "METHODOLOGIE",
    title: "Pourquoi le Score de Confiance ne compte pas les etoiles Google",
    excerpt: "La moyenne d'etoiles est un indicateur fragile. Voici comment notre redaction croise quatre sources avant de retenir un pourcentage.",
    author: "Antoine Delaunay",
    date: "15 avril 2026",
    readingTime: 7,
    slug: "methodologie-score-confiance-etoiles",
  },
  {
    category: "PORTRAIT",
    title: "Atelier Maurel, menuiserie d'art a Dijon depuis 1974",
    excerpt: "Quatre generations, deux compagnons du devoir, et l'une des trois medailles d'Or decernees cette annee en Cote-d'Or.",
    author: "Sarah Poitevin",
    date: "11 avril 2026",
    readingTime: 9,
    slug: "portrait-atelier-maurel-dijon",
  },
];

// Portrait vedette (Lauréat du mois)
const portraitVedette = {
  name: "Dubois & Fils , Plomberie generale",
  slug: "dubois-fils-plomberie-generale",
  ville: "Auxerre (89)",
  citation:
    "Nous refusons un chantier sur trois. Cela etonne les clients. Mais refuser, c'est aussi proteger la reputation qu'on a mise quarante ans a construire.",
  citationAuteur: "Jean-Paul Dubois, gerant",
  score: 9.4,
  fondation: 1984,
  salaries: 12,
  resume:
    "Entreprise familiale de plomberie generale basee a Auxerre depuis 1984. Specialisee en renovation de reseaux anciens et depannage d'urgence dans l'Yonne. Deux plombiers compagnons du devoir, trois apprentis. Certifiee RGE et Qualibat depuis 2011. Portrait Laureat 2026.",
};

// Top 3 du classement mis en avant (Plomberie Yonne)
const topClassementMisEnAvant = [
  {
    rank: 1, name: "Dubois & Fils", slug: "dubois-fils-plomberie-generale", score: 9.4,
    tier: "or" as const, ville: "Auxerre (89)", zoneIntervention: "Yonne, Cote-d'Or",
    ancienneteAnnees: 42, rge: true, qualibat: true,
    specialites: ["Reseaux anciens", "Depannage urgence", "Salles de bain"],
    resumeCourt: "Entreprise familiale plomberie generale Yonne, 4 generations, deux compagnons du devoir.",
    previewImages: [],
  },
  {
    rank: 2, name: "Lefevre Thermique", slug: "lefevre-thermique", score: 9.1,
    tier: "argent" as const, ville: "Sens (89)", zoneIntervention: "Nord Yonne",
    ancienneteAnnees: 22, rge: true, qualibat: false,
    specialites: ["Chaudieres gaz", "Pompes a chaleur", "Sanitaires"],
    resumeCourt: "Specialiste PAC et chaudieres gaz a condensation, certifie RGE depuis 2014.",
  },
  {
    rank: 3, name: "SARL Martin", slug: "sarl-martin-plombier", score: 8.7,
    tier: "gratuit" as const, ville: "Joigny (89)", zoneIntervention: "Joigny et alentours",
    ancienneteAnnees: 18, rge: false, qualibat: true,
    specialites: ["Depannage", "Pose sanitaires"],
    resumeCourt: "Artisan installe depuis 2008, qualification Qualibat confirmee.",
  },
];

// Titre SEO homepage , target 55-60 chars pour eviter la troncature Google
const title = `${siteConfig.name} , Classement des artisans de France`;
const description = siteConfig.description;

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  name: siteConfig.name,
  description: siteConfig.description,
  publisher: { "@type": "NewsMediaOrganization", name: siteConfig.name, url: siteConfig.url },
  hasPart: classementsPopulaires.map((c) => ({
    "@type": "ItemList",
    name: `Classement ${c.metier} ${c.dept}`,
    url: `${siteConfig.url}/${c.slug}/${c.deptSlug}/`,
  })),
  mainEntity: {
    "@type": "FAQPage",
    mainEntity: siteConfig.faq.map((q) => ({
      "@type": "Question",
      name: q.question,
      acceptedAnswer: { "@type": "Answer", text: q.answer },
    })),
  },
};
---

<BaseLayout title={title} description={description} jsonLd={jsonLd}>

  <!-- ═══════════════════ HERO ═══════════════════ -->
  <section class="hero-section" aria-label="Accueil éditorial">
    <TelescopeWatermark side="right" size={520} parallax={0.05} />

    <div class="container-wide hero-inner">
      <div class="hero-text">
        <div class="hero-overline fade-up">
          <span class="live-dot" aria-hidden="true"></span>
          <span class="meta-label-ink">Édition N°{siteConfig.editionNumber} &middot; {siteConfig.editionMonth} {siteConfig.editionYear}</span>
        </div>

        <h1 class="heading-hero hero-title">
          <span data-split-words>{siteConfig.baseline}</span><span class="hero-verified" aria-label="Média éditorial vérifié" title="Média éditorial vérifié , sources officielles INSEE, ADEME, Qualibat">
            <svg viewBox="0 0 28 28" width="60" height="60" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
              <defs>
                <linearGradient id="verif-grad" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%" stop-color="#1E3A52" />
                  <stop offset="100%" stop-color="#0F2335" />
                </linearGradient>
              </defs>
              <path d="M14 1.5 L16.5 3.2 L19.6 2.7 L20.7 5.6 L23.3 7 L22.7 10 L24 12.8 L22 15 L22 18 L19.4 19.2 L18 22 L15 22 L13 24 L11 22 L8 22 L6.6 19.2 L4 18 L4 15 L2 12.8 L3.3 10 L2.7 7 L5.3 5.6 L6.4 2.7 L9.5 3.2 L12 1.5 Z" fill="url(#verif-grad)" />
              <path d="M9 13.5 L12.5 17 L19 10" stroke="#F7F3EC" stroke-width="2.4" fill="none" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
            <span class="sr-only">Média éditorial vérifié</span>
          </span>
        </h1>

        <p class="lead hero-lead fade-up">
          Chaque mois, notre rédaction observe, vérifie et classe les meilleurs artisans de France. Sans algorithme opaque. Avec méthode. Avec patience.
        </p>

        <div class="hero-counter fade-up">
          <CounterLive initial={47382} label="professionnels sous observation" />
        </div>

        <div class="hero-cta-row fade-up">
          <a href="/selection/" class="btn btn-primary magnetic">Explorer la sélection</a>
          <a href="/methode/" class="btn btn-ghost">Consulter la méthode <span class="arrow">&rarr;</span></a>
        </div>

        <!-- Hero pills , credentials d'autorite -->
        <div class="hero-pills fade-up">
          <InfoPill variant="verified" label="Sources" value="INSEE" glossaireSlug="sirene" />
          <InfoPill variant="or" label="RGE" value="ADEME" glossaireSlug="rge" />
          <InfoPill variant="or" label="Qualibat" value="Vérifié" glossaireSlug="qualibat" />
          <InfoPill variant="info" label="Édition" value={`N°${siteConfig.editionNumber}`} />
          <InfoPill variant="neutral" label="Depuis" value="2026" />
        </div>
      </div>

      <div class="hero-seal scale-reveal">
        <Seal size={280} variant="or" animated={true} stamp={true} />
        <p class="hero-seal-caption meta-label-or">Sceau de la rédaction 2026</p>
      </div>
    </div>
  </section>

  <!-- ═══════════════════ À LA UNE ═══════════════════ -->
  <section class="section-alaune container-wide">
    <EditorialHeader category="À LA UNE CETTE SEMAINE" title="Observations de la rédaction" subtitle="Trois sujets que notre rédaction a jugés dignes d'être mis à la une cette semaine." />

    <div class="alaune-grid stagger-children">
      {alaUne.map((item, i) => (
        <article class={`alaune-card card-editorial curl ${i === 0 ? "alaune-card-hero" : ""}`}>
          <div class="alaune-visual" aria-hidden="true">
            <span class="alaune-visual-initial">{item.category.charAt(0)}</span>
            <svg width="100%" height="100%" viewBox="0 0 400 260" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice">
              <defs>
                <pattern id={`grid-${i}`} width="24" height="24" patternUnits="userSpaceOnUse">
                  <path d="M 24 0 L 0 0 0 24" fill="none" stroke="currentColor" stroke-width="0.4" />
                </pattern>
              </defs>
              <rect width="400" height="260" fill="var(--paper-warm)" />
              <rect width="400" height="260" fill={`url(#grid-${i})`} style="color: var(--ink-muted); opacity: 0.25" />
              <text x="50%" y="50%" text-anchor="middle" font-family="Instrument Serif, Georgia, serif" font-size="148" font-weight="400" fill="var(--ink)" opacity="0.12" dy=".38em" font-style="italic">{String(i+1).padStart(2, "0")}</text>
            </svg>
          </div>
          <div class="alaune-body">
            <p class="meta-label-or">{item.category}</p>
            <h3 class="alaune-title"><a href={`/observations/${item.slug}/`}>{item.title}</a></h3>
            <p class="alaune-excerpt">{item.excerpt}</p>
            <p class="byline folio">{item.author} &middot; {item.date} &middot; {item.readingTime} min</p>
          </div>
        </article>
      ))}
    </div>
  </section>

  <!-- ═══════════════════ PORTRAIT DE LA SEMAINE ═══════════════════ -->
  <section class="section-portrait">
    <div class="container-wide portrait-inner">
      <div class="portrait-meta slide-left">
        <span class="meta-label-or">Portrait Lauréat &middot; Avril 2026</span>
        <h2 class="heading-display portrait-title">
          <span class="italic-fraunces">{portraitVedette.name}</span>
        </h2>
        <p class="lead portrait-lead">
          Portrait long format de la seule entreprise de plomberie générale de l'Yonne distinguée d'une médaille d'Or cette année.
        </p>
        <ul class="portrait-stats">
          <li><span class="stat-val">{portraitVedette.score}</span><span class="stat-lbl">/ 10 Score de Confiance</span></li>
          <li><span class="stat-val">{new Date().getFullYear() - portraitVedette.fondation}</span><span class="stat-lbl">années d'activité</span></li>
          <li><span class="stat-val">{portraitVedette.salaries}</span><span class="stat-lbl">salariés et apprentis</span></li>
        </ul>
        <a href={`/pro/${portraitVedette.slug}/`} class="btn btn-observatoire magnetic">Lire le portrait complet</a>
      </div>

      <div class="portrait-citation slide-right">
        <PullQuote cite={portraitVedette.citationAuteur} align="left">
          {portraitVedette.citation}
        </PullQuote>
        <p class="portrait-resume prose">{portraitVedette.resume}</p>
      </div>
    </div>
  </section>

  <!-- ═══════════════════ LES 3 TEMPS DE LA MÉTHODE ═══════════════════ -->
  <section class="section-methode container-wide">
    <EditorialHeader category="LA MÉTHODE" title="Observer. Vérifier. Recommander." subtitle="Trois temps, six critères, une méthode publique, vérifiable, décrite par écrit." align="center" />

    <ol class="methode-trois-temps stagger-children">
      <li class="methode-temps card-editorial">
        <span class="rank-num rank-outlined">01</span>
        <h3>Observer</h3>
        <p>Nous cartographions les 47 382 artisans référencés au Sirene sur nos zones pilotes. Données publiques uniquement. Sources citées.</p>
      </li>
      <li class="methode-temps card-editorial">
        <span class="rank-num rank-outlined">02</span>
        <h3>Vérifier</h3>
        <p>Chaque candidat est confronté à six critères documentés : ancienneté, RGE, avis, SIRET, site web, portfolio. Pas de score, pas de publication.</p>
      </li>
      <li class="methode-temps card-editorial">
        <span class="rank-num rank-outlined">03</span>
        <h3>Recommander</h3>
        <p>La rédaction classe. Médailles Or et Argent décernées après examen éditorial. Archivées chaque année. Consultables indéfiniment.</p>
      </li>
    </ol>

    <p class="methode-foot-cta">
      <a href="/methode/" class="btn-ghost">Lire la méthode complète <span class="arrow">&rarr;</span></a>
    </p>
  </section>

  <InkBleedDivider variant="hairline" />

  <!-- ═══════════════════ CLASSEMENTS POPULAIRES ═══════════════════ -->
  <section class="section-classements container-wide">
    <EditorialHeader category="LA SÉLECTION" title="Les classements les plus consultés" subtitle="Huit classements départementaux les plus consultés cette semaine par les lecteurs." />

    <div class="classements-grid stagger-children">
      {classementsPopulaires.map((c) => (
        <a href={`/${c.slug}/${c.deptSlug}/`} class="classement-card card-editorial curl">
          <span class="meta-label">{c.dept}</span>
          <h3 class="classement-title italic-fraunces">{c.metier}s</h3>
          <p class="classement-count folio">{c.count} observations &middot; édition en cours</p>
          <span class="classement-arrow">&rarr;</span>
        </a>
      ))}
    </div>

    <p class="classements-tous">
      <a href="/selection/" class="btn-ghost">Voir tous les classements <span class="arrow">&rarr;</span></a>
    </p>
  </section>

  <InkBleedDivider variant="ornament" />

  <!-- ═══════════════════ TOP 3 , EXTRAIT D'UN CLASSEMENT ═══════════════════ -->
  <section class="section-top3 container-wide">
    <EditorialHeader category="EXTRAIT DU CLASSEMENT" title="Plomberie générale dans l'Yonne" subtitle="Trois premières places du classement, extrait de l'édition mensuelle." align="center" />

    <div class="top3-list stagger-children">
      {topClassementMisEnAvant.map((p) => (
        <div data-rank-item={p.rank}>
          <ProCard {...p} avisMoyen={p.rank === 1 ? 4.8 : p.rank === 2 ? 4.5 : 4.3} avisNombre={p.rank === 1 ? 132 : p.rank === 2 ? 89 : 47} />
        </div>
      ))}
    </div>

    <p class="top3-link">
      <a href="/plombier/yonne-89/" class="btn btn-outline magnetic">Consulter le classement intégral <span class="arrow">&rarr;</span></a>
    </p>
  </section>

  <!-- ═══════════════════ STATS MENSUELLES ═══════════════════ -->
  <section class="section-stats">
    <div class="container-wide stats-grid-v2 stagger-children">
      <div class="stat-block-v2">
        <div class="stat-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="1.3">
            <circle cx="12" cy="12" r="9" />
            <circle cx="12" cy="12" r="4" fill="currentColor" opacity="0.15" />
            <circle cx="12" cy="12" r="1.5" fill="currentColor" />
          </svg>
        </div>
        <p class="stat-num-v2" data-count="47382">47 382</p>
        <p class="stat-label-v2">professionnels sous observation</p>
      </div>
      <div class="stat-block-v2">
        <div class="stat-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="1.3">
            <path d="M4 7 L12 3 L20 7 V 12 C 20 16 16 20 12 21 C 8 20 4 16 4 12 Z" />
            <path d="M9 12 L11 14 L15 10" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.6" />
          </svg>
        </div>
        <p class="stat-num-v2" data-count="6">6</p>
        <p class="stat-label-v2">critères de scoring publics</p>
      </div>
      <div class="stat-block-v2">
        <div class="stat-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="1.3">
            <path d="M12 3 C 8 3 5 6 5 10 C 5 14 12 21 12 21 C 12 21 19 14 19 10 C 19 6 16 3 12 3 Z" />
            <circle cx="12" cy="10" r="3" />
          </svg>
        </div>
        <p class="stat-num-v2" data-count="3">3</p>
        <p class="stat-label-v2">départements pilotes cette édition</p>
      </div>
      <div class="stat-block-v2">
        <div class="stat-icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="1.3">
            <rect x="4" y="5" width="16" height="16" rx="2" />
            <path d="M4 10 H 20" />
            <circle cx="8" cy="14" r="1" fill="currentColor" />
            <circle cx="12" cy="14" r="1" fill="currentColor" />
            <circle cx="16" cy="14" r="1" fill="currentColor" />
          </svg>
        </div>
        <p class="stat-num-v2" data-count="128">128</p>
        <p class="stat-label-v2">dossiers candidats examinés</p>
      </div>
    </div>
  </section>

  <!-- ═══════════════════ ESPACE PRO , card premium ═══════════════════ -->
  <section class="section-pro-v2">
    <div class="container-editorial">
      <div class="pro-v2-card blur-reveal">
        <div class="pro-v2-decor" aria-hidden="true">
          <svg viewBox="0 0 120 120" width="120" height="120" fill="none" stroke="currentColor" stroke-width="0.8">
            <circle cx="60" cy="60" r="48" opacity="0.3" />
            <circle cx="60" cy="60" r="38" opacity="0.25" />
            <circle cx="60" cy="60" r="28" opacity="0.2" />
            <g transform="translate(60 60)">
              <path d="M0 -22 L6 0 L-6 0 Z" fill="currentColor" opacity="0.9" />
              <path d="M0 22 L-6 0 L6 0 Z" fill="currentColor" opacity="0.4" />
              <circle cx="0" cy="0" r="3" fill="currentColor" />
            </g>
          </svg>
        </div>
        <div class="pro-v2-body">
          <span class="meta-label-or">Candidater à l'édition {siteConfig.editionYear}</span>
          <h2 class="heading-display pro-v2-title">Votre entreprise mérite-t-elle de figurer dans notre guide ?</h2>
          <p class="lead">
            Chaque année, notre rédaction examine quelques milliers de dossiers. Les meilleurs reçoivent la médaille d'Argent ou d'Or. La candidature est gratuite. Le passage en <GlossaireTerm slug="portrait-laureat">Portrait Lauréat</GlossaireTerm> reste optionnel.
          </p>
          <div class="pro-v2-actions">
            <a href="/candidater/" class="btn btn-primary magnetic">Soumettre ma candidature</a>
            <a href="/methode/" class="btn btn-ghost">Consulter la méthode <span class="arrow">&rarr;</span></a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <InkBleedDivider variant="compass" />

  <!-- ═══════════════════ DERNIÈRES OBSERVATIONS ═══════════════════ -->
  <section class="section-observations container-wide">
    <EditorialHeader category="NOS OBSERVATIONS" title="Dernières publications de la rédaction" />
    <div class="observations-list">
      {alaUne.map((o) => (
        <a class="observation-row curl" href={`/observations/${o.slug}/`}>
          <div class="observation-meta">
            <span class="meta-label-or">{o.category}</span>
            <span class="folio">{o.date}</span>
          </div>
          <h3 class="observation-title">{o.title}</h3>
          <p class="observation-excerpt">{o.excerpt}</p>
          <span class="observation-arrow">&rarr;</span>
        </a>
      ))}
    </div>
    <p><a href="/observations/" class="btn-ghost">Toutes les observations <span class="arrow">&rarr;</span></a></p>
  </section>

  <!-- ═══════════════════ FAQ ═══════════════════ -->
  <section class="section-faq">
    <div class="container-editorial">
      <EditorialHeader category="QUESTIONS FRÉQUENTES" title="Ce que nous répondons, publiquement" align="center" />
      <div class="faq-list stagger-children">
        {siteConfig.faq.map((q, i) => (
          <details class="faq-item">
            <summary>
              <span class="folio faq-num">{String(i+1).padStart(2, "0")}</span>
              <span class="faq-question">{q.question}</span>
              <span class="faq-toggle" aria-hidden="true">+</span>
            </summary>
            <div class="faq-answer"><p>{q.answer}</p></div>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════════════ NEWSLETTER ═══════════════════ -->
  <section class="section-newsletter">
    <div class="container-editorial newsletter-inner">
      <Seal size={72} variant="or" animated={true} />
      <h2 class="heading-display newsletter-title">La lettre de L'Observatoire</h2>
      <p class="lead">
        Chaque mois, notre rédaction adresse une lettre à ses lecteurs. Une observation majeure, un portrait, une question de méthode. Pas de publicité, pas de tracking invasif. Désabonnement en un clic.
      </p>
      <form class="newsletter-form" method="post" action="#">
        <label class="sr-only" for="nl-email">Adresse email</label>
        <input id="nl-email" type="email" name="email" required placeholder="votre adresse email" />
        <button type="submit" class="btn btn-observatoire">S'abonner</button>
      </form>
      <p class="meta-label newsletter-meta">Gratuit. Ouvert à tous. Archives consultables.</p>
    </div>
  </section>

</BaseLayout>

<style>
  /* ═══════ HERO ═══════ */
  .hero-section {
    position: relative;
    padding: clamp(3rem, 7vw, 7rem) 0 clamp(3rem, 6vw, 6rem);
    overflow: hidden;
  }
  .hero-inner {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: clamp(2rem, 6vw, 5rem);
    align-items: center;
  }
  @media (max-width: 960px) {
    .hero-inner { grid-template-columns: 1fr; }
    .hero-seal { order: -1; justify-self: center; }
  }
  .hero-overline {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.4rem 0.75rem 0.4rem 0.6rem;
    border: 1px solid rgba(26, 22, 20, 0.15);
    border-radius: 2px;
    background: rgba(247, 243, 236, 0.75);
    margin-bottom: 1.6rem;
  }
  .hero-title {
    margin: 0.4rem 0 1.5rem;
    max-width: 16ch;
    position: relative;
  }
  .hero-verified {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    vertical-align: baseline;
    margin-left: -0.05em;
    margin-right: 0;
    position: relative;
    top: -0.05em;
    cursor: help;
    animation: verif-pop 900ms cubic-bezier(0.22, 1, 0.36, 1) 1800ms both;
  }
  .hero-verified svg {
    display: block;
    width: clamp(46px, 6vw, 78px);
    height: auto;
    filter: drop-shadow(0 4px 10px rgba(15, 35, 53, 0.35));
    transition: transform 300ms var(--ease-out-quart);
  }
  .hero-verified:hover svg {
    transform: rotate(10deg) scale(1.12);
  }
  @keyframes verif-pop {
    0%   { opacity: 0; transform: scale(0.4) rotate(-18deg); }
    60%  { opacity: 1; transform: scale(1.15) rotate(6deg); }
    100% { opacity: 1; transform: scale(1) rotate(0); }
  }
  .sr-only {
    position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
    overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;
  }
  @media (prefers-reduced-motion: reduce) {
    .hero-verified { animation: none; }
    .hero-verified:hover svg { transform: none; }
  }
  .hero-lead {
    max-width: 36rem;
    margin: 0 0 1.8rem;
  }
  .hero-counter {
    margin: 0 0 1.8rem;
    padding: 1rem 1.2rem;
    border-left: 2px solid var(--or);
    background: rgba(239, 231, 214, 0.4);
  }
  .hero-cta-row {
    display: flex;
    gap: 1.2rem;
    align-items: center;
    flex-wrap: wrap;
    margin-bottom: 2rem;
  }
  .hero-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
    margin-top: 0.5rem;
  }
  .hero-seal {
    position: relative;
    display: grid;
    place-items: center;
    gap: 0.8rem;
  }
  .hero-seal-caption {
    text-align: center;
    margin-top: 0.5rem;
  }

  /* ═══════ À LA UNE ═══════ */
  .section-alaune { padding: clamp(2rem, 3vw, 3rem) 0 clamp(2rem, 3.5vw, 3.5rem); }
  .alaune-grid {
    display: grid;
    grid-template-columns: 1.4fr 1fr 1fr;
    gap: 1.8rem;
    align-items: start;
  }
  @media (max-width: 960px) {
    .alaune-grid { grid-template-columns: 1fr 1fr; }
    .alaune-card-hero { grid-column: 1 / -1; }
  }
  @media (max-width: 640px) {
    .alaune-grid { grid-template-columns: 1fr; }
    .alaune-card-hero { grid-column: 1; }
  }
  .alaune-card {
    padding: 0; overflow: hidden;
    border-radius: 4px 28px 4px 4px;
    transition: transform 420ms var(--ease-out-expo), box-shadow 420ms var(--ease-out-expo), border-color 420ms var(--ease-out-expo);
    position: relative;
  }
  .alaune-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 22px 44px -20px rgba(26, 22, 20, 0.22);
    border-color: rgba(184, 134, 61, 0.5);
  }
  .alaune-card::after {
    content: "";
    position: absolute;
    top: 0; right: 0;
    width: 60px; height: 60px;
    background: radial-gradient(circle at 100% 0%, rgba(184, 134, 61, 0.15) 0%, transparent 70%);
    pointer-events: none;
    opacity: 0;
    transition: opacity 420ms var(--ease-out-expo);
  }
  .alaune-card:hover::after { opacity: 1; }
  .alaune-visual { position: relative; aspect-ratio: 4 / 3; background: var(--paper-warm); overflow: hidden; }
  .alaune-visual-initial { display: none; }
  .alaune-card-hero .alaune-visual { aspect-ratio: 16 / 9; }
  .alaune-card-hero { border-radius: 4px 36px 4px 4px; }
  .alaune-body { padding: 1.4rem 1.6rem 1.7rem; }
  .alaune-title {
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 2vw, 1.7rem);
    font-weight: 500;
    letter-spacing: -0.015em;
    line-height: 1.15;
    margin: 0.4rem 0 0.6rem;
  }
  .alaune-card-hero .alaune-title { font-size: clamp(1.6rem, 2.8vw, 2.1rem); }
  .alaune-title a { color: var(--ink); text-decoration: none; border-bottom: 1px solid transparent; transition: border-color 200ms; }
  .alaune-title a:hover { border-color: var(--or); }
  .alaune-excerpt { color: var(--ink-soft); font-size: 0.98rem; line-height: 1.55; margin-bottom: 0.7rem; }

  /* ═══════ PORTRAIT VEDETTE ═══════ */
  .section-portrait { padding: clamp(2rem, 5vw, 5rem) 0; background: var(--paper-warm); }
  .portrait-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2rem, 5vw, 4rem);
    align-items: start;
  }
  @media (max-width: 860px) { .portrait-inner { grid-template-columns: 1fr; } }
  .portrait-title { margin: 0.5rem 0 1rem; }
  .portrait-lead { margin-bottom: 1.4rem; }
  .portrait-stats {
    list-style: none;
    padding: 0;
    margin: 0 0 1.8rem;
    display: flex;
    gap: 2.5rem;
    flex-wrap: wrap;
  }
  .portrait-stats li { display: flex; flex-direction: column; gap: 0.1rem; }
  .stat-val { font-family: var(--font-display); font-size: 1.6rem; font-weight: 600; color: var(--ink); letter-spacing: -0.01em; }
  .stat-lbl { font-size: 0.78rem; color: var(--ink-muted); text-transform: uppercase; letter-spacing: 0.12em; }
  .portrait-resume { font-size: 0.98rem; }

  /* ═══════ MÉTHODE 3 TEMPS ═══════ */
  .section-methode { padding: clamp(2rem, 3vw, 3rem) 0 clamp(1.5rem, 2.5vw, 2.5rem); }
  .methode-trois-temps {
    list-style: none;
    padding: 0;
    margin: 2rem 0 2rem;
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 1.8rem;
  }
  @media (max-width: 860px) { .methode-trois-temps { grid-template-columns: 1fr; } }
  .methode-temps {
    padding: 1.8rem 1.8rem 2rem;
    display: grid;
    gap: 0.5rem;
    background: var(--paper);
    border-radius: 4px 26px 4px 4px;
    transition: transform 400ms var(--ease-out-expo), box-shadow 400ms var(--ease-out-expo), border-color 400ms;
    position: relative;
    overflow: hidden;
  }
  .methode-temps::before {
    content: "";
    position: absolute;
    bottom: 0; left: 0;
    width: 100%; height: 3px;
    background: linear-gradient(90deg, var(--or) 0%, var(--or-deep) 80%, transparent);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 500ms var(--ease-out-expo);
  }
  .methode-temps:hover {
    transform: translateY(-3px);
    box-shadow: 0 18px 40px -22px rgba(26, 22, 20, 0.22);
    border-color: rgba(184, 134, 61, 0.5);
  }
  .methode-temps:hover::before { transform: scaleX(1); }
  .methode-temps .rank-num { font-size: 3.5rem; line-height: 1; }
  .methode-temps h3 { font-size: 1.5rem; margin: 0; font-weight: 500; }
  .methode-temps p { font-size: 0.96rem; color: var(--ink-soft); margin: 0; }
  .methode-foot-cta { text-align: center; margin-top: 1.5rem; }

  /* ═══════ CLASSEMENTS POPULAIRES ═══════ */
  .section-classements { padding: clamp(1rem, 2vw, 2rem) 0 clamp(1rem, 2vw, 2rem); }
  .classements-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
    margin-top: 1.6rem;
  }
  @media (max-width: 960px) { .classements-grid { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 480px) { .classements-grid { grid-template-columns: 1fr; } }
  .classement-card {
    padding: 1.3rem 1.3rem 1.4rem;
    text-decoration: none;
    color: var(--ink);
    position: relative;
    display: grid;
    gap: 0.4rem;
    border-radius: 4px 22px 4px 4px;
    transition: all 380ms var(--ease-out-expo);
    overflow: hidden;
  }
  .classement-card::before {
    content: "";
    position: absolute;
    top: 0; right: 0;
    width: 44px; height: 44px;
    background: radial-gradient(circle at 100% 0%, rgba(184, 134, 61, 0.18) 0%, transparent 60%);
    pointer-events: none;
    opacity: 0;
    transition: opacity 400ms var(--ease-out-expo);
  }
  .classement-card:hover::before { opacity: 1; }
  .classement-card .classement-title {
    font-family: var(--font-display);
    font-size: 1.45rem;
    font-weight: 500;
    color: var(--ink);
    margin: 0.2rem 0 0.1rem;
    letter-spacing: -0.012em;
  }
  .classement-count { color: var(--ink-muted); margin: 0; }
  .classement-arrow { position: absolute; top: 1.3rem; right: 1.3rem; color: var(--or-deep); font-size: 1.1rem; transition: transform 240ms var(--ease-out-quart); }
  .classement-card:hover .classement-arrow { transform: translate(4px, -2px); }
  .classements-tous { text-align: center; margin-top: 1.6rem; }

  /* ═══════ TOP 3 ═══════ */
  .section-top3 { padding: clamp(1rem, 2vw, 2rem) 0 clamp(2rem, 4vw, 4rem); }
  .top3-list { display: grid; gap: 1.1rem; }
  .top3-link { text-align: center; margin-top: 2rem; }

  /* ═══════ STATS ═══════ */
  .section-stats {
    background: var(--ink);
    color: var(--paper);
    padding: clamp(3.5rem, 5vw, 5.5rem) 0;
    position: relative;
    overflow: hidden;
  }
  .section-stats::before {
    content: "";
    position: absolute;
    top: 0; left: 50%;
    transform: translateX(-50%);
    width: 80px; height: 2px;
    background: var(--or);
  }
  .section-stats::after {
    content: "";
    position: absolute;
    inset: 0;
    background-image:
      radial-gradient(circle at 20% 30%, rgba(184, 134, 61, 0.06) 0%, transparent 40%),
      radial-gradient(circle at 80% 70%, rgba(184, 134, 61, 0.04) 0%, transparent 40%);
    pointer-events: none;
  }
  .stats-grid-v2 {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.2rem;
    position: relative;
    z-index: 1;
  }
  @media (max-width: 860px) { .stats-grid-v2 { grid-template-columns: 1fr 1fr; gap: 1rem; } }
  @media (max-width: 420px) { .stats-grid-v2 { grid-template-columns: 1fr; } }
  .stat-block-v2 {
    display: grid;
    gap: 0.55rem;
    padding: 1.5rem 1.4rem 1.6rem;
    background: rgba(247, 243, 236, 0.03);
    border: 1px solid rgba(247, 243, 236, 0.08);
    border-radius: 4px 22px 4px 4px;
    transition: background 400ms var(--ease-out-expo), border-color 400ms var(--ease-out-expo), transform 400ms var(--ease-out-expo);
  }
  .stat-block-v2:hover {
    background: rgba(247, 243, 236, 0.06);
    border-color: rgba(184, 134, 61, 0.35);
    transform: translateY(-3px);
  }
  .stat-icon {
    color: var(--or-soft);
    width: 36px;
    height: 36px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: rgba(184, 134, 61, 0.08);
    border-radius: 8px;
    margin-bottom: 0.2rem;
  }
  .stat-num-v2 {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 4vw, 3.8rem);
    font-weight: 400;
    letter-spacing: -0.025em;
    color: var(--or-soft);
    margin: 0;
    line-height: 1;
    font-variant-numeric: tabular-nums;
  }
  .stat-label-v2 {
    font-size: 0.82rem;
    color: rgba(247, 243, 236, 0.75);
    margin: 0;
    line-height: 1.4;
  }

  /* ═══════ PRO CTA V2 ═══════ */
  .section-pro-v2 {
    padding: clamp(3rem, 5vw, 5rem) 0;
  }
  .pro-v2-card {
    background: linear-gradient(135deg, var(--paper-warm) 0%, var(--paper) 65%);
    border: 2px solid var(--or);
    border-radius: 4px 48px 4px 4px;
    padding: 2.2rem 2.6rem 2.4rem;
    position: relative;
    overflow: hidden;
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 2.2rem;
    align-items: center;
  }
  .pro-v2-card::before {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 88% 18%, rgba(184, 134, 61, 0.14) 0%, transparent 50%);
    pointer-events: none;
  }
  @media (max-width: 760px) {
    .pro-v2-card { grid-template-columns: 1fr; text-align: center; }
    .pro-v2-decor { justify-self: center; }
  }
  .pro-v2-decor { color: var(--or); position: relative; z-index: 1; opacity: 0.95; }
  .pro-v2-body { position: relative; z-index: 1; }
  .pro-v2-title {
    font-size: clamp(1.6rem, 3vw, 2.3rem);
    margin: 0.45rem 0 0.9rem;
    line-height: 1.1;
  }
  .pro-v2-body .lead { margin: 0 0 1.4rem; max-width: 44rem; }
  .pro-v2-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }
  @media (max-width: 760px) { .pro-v2-actions { justify-content: center; } }

  /* ═══════ OBSERVATIONS LIST ═══════ */
  .section-observations { padding: clamp(1.5rem, 3vw, 3rem) 0 clamp(2rem, 3.5vw, 3.5rem); }
  .observations-list { display: grid; gap: 0; margin-top: 1rem; }
  .observation-row {
    display: block;
    padding: 1.6rem 0 1.7rem;
    border-top: 1px solid rgba(26, 22, 20, 0.1);
    text-decoration: none;
    color: inherit;
    position: relative;
    transition: padding-left 280ms var(--ease-out-quart), background 280ms;
  }
  .observation-row:last-child { border-bottom: 1px solid rgba(26, 22, 20, 0.1); }
  .observation-row:hover { padding-left: 0.8rem; background: rgba(239, 231, 214, 0.3); }
  .observation-meta { display: flex; gap: 1rem; align-items: baseline; margin-bottom: 0.35rem; }
  .observation-title {
    font-family: var(--font-display);
    font-size: clamp(1.3rem, 2vw, 1.7rem);
    font-weight: 500;
    letter-spacing: -0.014em;
    margin: 0 0 0.45rem;
    color: var(--ink);
  }
  .observation-excerpt { color: var(--ink-soft); margin: 0 0 0.2rem; max-width: 54rem; }
  .observation-arrow {
    position: absolute; right: 0; top: 50%;
    transform: translateY(-50%); color: var(--or-deep);
    transition: transform 280ms var(--ease-out-quart);
  }
  .observation-row:hover .observation-arrow { transform: translate(6px, -50%); }

  /* ═══════ FAQ ═══════ */
  .section-faq { padding: clamp(3rem, 5vw, 5rem) 0; background: var(--paper-warm); }
  .faq-list { display: grid; gap: 0.4rem; margin-top: 1.6rem; }
  .faq-item {
    background: var(--paper);
    border: 1px solid rgba(26, 22, 20, 0.08);
    border-radius: 2px;
    padding: 0;
    transition: border-color 240ms, box-shadow 240ms;
  }
  .faq-item[open] { border-color: rgba(184, 134, 61, 0.4); box-shadow: var(--shadow-paper); }
  .faq-item summary {
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 1rem;
    align-items: center;
    padding: 1.1rem 1.4rem;
    cursor: pointer;
    list-style: none;
  }
  .faq-item summary::-webkit-details-marker { display: none; }
  .faq-num { color: var(--or-deep); }
  .faq-question {
    font-family: var(--font-display);
    font-size: 1.12rem;
    font-weight: 500;
    color: var(--ink);
    letter-spacing: -0.012em;
  }
  .faq-toggle {
    font-family: var(--font-display);
    font-size: 1.5rem;
    color: var(--or-deep);
    transition: transform 240ms var(--ease-out-quart);
  }
  .faq-item[open] .faq-toggle { transform: rotate(45deg); }
  .faq-answer { padding: 0 1.4rem 1.3rem 3.4rem; color: var(--ink-soft); font-size: 0.98rem; line-height: 1.6; }
  .faq-answer p { margin: 0; }

  /* ═══════ NEWSLETTER ═══════ */
  .section-newsletter {
    padding: clamp(4rem, 6vw, 6rem) 0;
    background: linear-gradient(180deg, var(--paper) 0%, var(--paper-warm) 100%);
  }
  .newsletter-inner {
    text-align: center;
    display: grid;
    gap: 0.9rem;
    place-items: center;
  }
  .newsletter-title { margin: 0.4rem 0 0.4rem; }
  .newsletter-inner .lead { margin-inline: auto; }
  .newsletter-form {
    display: flex;
    gap: 0.6rem;
    width: 100%;
    max-width: 28rem;
    margin: 1.2rem auto 0.6rem;
    flex-wrap: wrap;
  }
  .newsletter-form input {
    flex: 1 1 220px;
    padding: 0.85rem 1.1rem;
    font-family: var(--font-body);
    font-size: 0.95rem;
    border: 1px solid rgba(26, 22, 20, 0.18);
    background: var(--paper);
    border-radius: 2px;
    color: var(--ink);
    min-width: 0;
  }
  .newsletter-form input:focus { outline: 2px solid var(--or); outline-offset: 1px; }
  .newsletter-meta { color: var(--ink-muted); font-size: 0.7rem; }

  .sr-only {
    position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
    overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;
  }
</style>
